CREATE TABLE IF NOT EXISTS Character (
    id INTEGER PRIMARY KEY,
    name TEXT,
    height INTEGER,
    mass INTEGER,
    hair_color TEXT,
    skin_color TEXT,
    eye_color TEXT,
    birth_year INTEGER
);